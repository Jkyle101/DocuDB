# Network
PORT=3001
HOST=0.0.0.0

# MongoDB Atlas
MONGO_URI=mongodb+srv://<docudb>:<Jkyle_taghoy1009.>@cluster0.hewognp.mongodb.net/?appName=Cluster0

# Frontend + CORS
FRONTEND_URL=https://docudb.site
CORS_ORIGINS=https://docudb.site,https://www.docudb.site
CORS_ALLOW_LAN=false
# Optional single-origin fallback:
# CORS_ORIGIN=https://docudb.site

# SMTP (optional, needed for email notifications)
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=no-reply@docudb.site
SMTP_PASS=<smtp_password>
SMTP_FROM_NAME=DocuDB

# Optional app secret (kept for compatibility)
JWT_SECRET=<long_random_secret>
